"""ARIA agents: Planner, Reasoner, Executor, and the orchestrating root agent.

These are defined with Google ADK and only imported in live mode (when
google-adk is installed and Gemini credentials are present). The demo engine
uses the same prompts directly against the Gemini API so the architecture is
identical either way.
"""
